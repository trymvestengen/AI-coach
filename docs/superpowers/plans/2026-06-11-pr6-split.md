# PR #6-splitt: Implementasjonsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Splitte PR #6 (`feat/onboarding-redesign`, 233 filer, +61k/−30k, 186 commits) i 6 stablede PR-er per delflate, der hvert nivå passerer `make check` og toppen av stacken er tre-identisk med dagens verifiserte branch.

**Architecture:** Stablede PR-er (hver PR baseres på forrige split-branch, merges i rekkefølge 1→6). Hver slice bygges ved fil-snapshot fra `feat/onboarding-redesign` (`git checkout feat/onboarding-redesign -- <paths>`), pluss minimale lim-redigeringer i delte filer (`api/app/main.py`, `api/tests/conftest.py`). Siste slice tar *alt gjenstående* mekanisk (`git rm -rq . && git checkout feat/onboarding-redesign -- .`), som garanterer at sluttilstanden er identisk med den allerede reviewede og prod-verifiserte branchen.

**Tech Stack:** git (checkout-pathspec-slicing), gh CLI, `make check` (tsc, eslint, vitest 130, pytest 250).

> **REVISJON 2026-06-11 (etter merge-forsoningen):** main er merget inn i branchen
> (35cb5c5) — merge-base er nå mains tip, så slicing fra main er ren. Migrasjonene er
> renummerert (gamle 009–016 → 010–017, + ny 018), tallene under er oppdatert.
> Skjema-docs-CI-gaten krever at `docs/ARCHITECTURE.md` endres i samme PR som
> migrasjoner — hver slice med migrasjoner tar med SINE rader i migrasjonstabellen
> (lim-edit); full docs-sync lander i slice 6. `.codegraph/` ligger i
> `.git/info/exclude` så `git add -A` ikke drar den med. Sluttverifisering: 265
> backend-tester (ikke 250).

**Nøkkelfakta som styrer designet:**
- Backend-tester mocker DB (`conftest.py` AsyncMock) — migrasjoner trengs ikke for grønne tester, kun for logisk gruppering i review.
- Prod kjører allerede branch-tip (alle migrasjoner 009–017 er applisert) — merge-rekkefølge til main er trygg for prod.
- `web/src/lib/api.ts` importerer kun `./supabase` — kan shippes i sin helhet tidlig (ekstra ubrukte funksjoner er harmløse for typecheck).
- Program-komponenter importerer `@/components/exercises/ExerciseDetailModal` og `@/components/brand/Icon` → exercise-db + web-foundation må ligge under program i stacken.
- Lim i delte filer er minimalt: `conftest.py` = 2 nye linjer (program_folders → slice 3, body_metrics → slice 6); `api/app/main.py` = 3 hunks (dotenv-override → slice 1, program_folders → slice 3, body_metrics → slice 6).

**Stack-oversikt:**

| # | Branch | Innhold | Base |
|---|--------|---------|------|
| 1 | `split/1-api-tools` | Tool-refaktor + nye coach-tools + chat-orkestrering (kun backend) | `main` |
| 2 | `split/2-exercise-db` | Exercise-DB v2 (free-exercise-db erstatter wger) + web-foundation | `split/1` |
| 3 | `split/3-program` | Program-tab restrukturering (mapper, wizard, detail, workout-run) | `split/2` |
| 4 | `split/4-onboarding-login` | Onboarding-wizard + login-redesign + middleware | `split/3` |
| 5 | `split/5-profil` | Profil-redesign (ProfileSettings, sheets, avatar) | `split/4` |
| 6 | `split/6-rest` | Chat-UI, body metrics, historikk/kalender, hjem, PWA, docs | `split/5` |

**Fallback-regel ved rød `make check` i en slice:** Feilen vil nesten alltid være en manglende import (fil ligger i senere slice). Hvis filen er en løvfil (ingen videre avhengigheter ut av slicen): flytt den inn i gjeldende slice og noter det i PR-beskrivelsen. Hvis ikke: stopp og revurder slice-grensen — ikke hack inn stubs.

**Felles verifisering per slice (gjentas i hver task):**
```bash
make check   # forventet: exit 0, 0 lint-errors, alle tester grønne
```
Antall tester vokser per slice; sluttnivå (slice 6) skal vise 130 web + 250 backend, identisk med baseline.

---

### Task 1: `split/1-api-tools` — backend tool-refaktor + chat-orkestrering

**Files:** (snapshot fra `feat/onboarding-redesign` der annet ikke er nevnt)
- `api/app/constants.py`, `api/app/db.py`
- `api/app/services/coach.py`, `api/app/services/memory.py`
- `api/app/tools/definitions.py`, `api/app/tools/dispatcher.py` (ny), `api/app/tools/result_links.py` (ny)
- `api/app/tools/handlers/` — hele pakken (14 filer, inkl. flyttet `memory_handlers.py`)
- Slett: `api/app/tools/handlers.py` (gammel monolitt), `api/app/tools/memory_handlers.py` (flyttet)
- `api/app/routers/chat.py`, `api/app/routers/chat_sessions.py`
- `api/db/migrations/011_user_notes.sql`, `017_body_metrics.sql`, `018_drop_duplicate_memory_policies.sql`
- Tester: `test_chat.py`, `test_chat_stream.py`, `test_memory_handlers.py`, `test_memory_service.py`, `test_tools.py`, `test_workout_tools.py`, `test_create_program_tool.py`, `test_tool_user_scoping.py` (MÅ med — mains versjon importerer den gamle handler-strukturen) + nye `test_tools_{constraint,equipment,folder,injury,preference,profile,program,social,workout}.py`
- Lim: `api/tests/conftest.py` (branch-versjon MINUS de to nye monkeypatch-linjene), `api/app/main.py` (kun dotenv-override-hunken), `docs/ARCHITECTURE.md` (+ rader 011/017/018 i migrasjonstabellen)

- [ ] **Step 1: Opprett branch fra main**
```bash
cd ~/dev/ai-coach
git checkout -b split/1-api-tools main
```

- [ ] **Step 2: Hent slice-filene fra feature-branchen**
```bash
git checkout feat/onboarding-redesign -- \
  api/app/constants.py api/app/db.py \
  api/app/services/coach.py api/app/services/memory.py \
  api/app/tools/definitions.py api/app/tools/dispatcher.py api/app/tools/result_links.py \
  api/app/tools/handlers/ \
  api/app/routers/chat.py api/app/routers/chat_sessions.py \
  api/db/migrations/011_user_notes.sql api/db/migrations/017_body_metrics.sql api/db/migrations/018_drop_duplicate_memory_policies.sql \
  api/tests/conftest.py api/tests/test_tool_user_scoping.py \
  api/tests/test_chat.py api/tests/test_chat_stream.py \
  api/tests/test_memory_handlers.py api/tests/test_memory_service.py \
  api/tests/test_tools.py api/tests/test_workout_tools.py api/tests/test_create_program_tool.py \
  api/tests/test_tools_constraint.py api/tests/test_tools_equipment.py api/tests/test_tools_folder.py \
  api/tests/test_tools_injury.py api/tests/test_tools_preference.py api/tests/test_tools_profile.py \
  api/tests/test_tools_program.py api/tests/test_tools_social.py api/tests/test_tools_workout.py
git rm -q api/app/tools/handlers.py api/app/tools/memory_handlers.py
```

- [ ] **Step 3: Lim — fjern de to router-patchene fra conftest.py som peker på routere som ikke finnes ennå**

Fjern (med Edit) disse to linjene fra `api/tests/conftest.py` (de gjeninnføres i slice 3 og 6):
```python
    monkeypatch.setattr("app.routers.program_folders.get_current_user_id", lambda r: TEST_USER_ID)
    monkeypatch.setattr("app.routers.body_metrics.get_current_user_id", lambda r: TEST_USER_ID)
```

- [ ] **Step 4: Lim — appliser kun dotenv-hunken i main.py**

`api/app/main.py` røres IKKE av checkout (står på main-versjon). Endre med Edit:
```python
# fra:
load_dotenv()
# til:
# override=True so .env wins over shell env vars (e.g. Claude Code CLI exports
# ANTHROPIC_API_KEY pointing at its own credentials, which we don't want here).
load_dotenv(override=True)
```

- [ ] **Step 5: Verifiser at coach.py/chat.py ikke importerer noe utenfor slicen**
```bash
cd api && .venv/bin/python -c "import app.main" && cd ..
```
Forventet: ingen ImportError. Ved ImportError: bruk fallback-regelen.

- [ ] **Step 6: Kjør make check**
```bash
make check
```
Forventet: exit 0. Web-delen er uendret fra main; backend-tester inkluderer alle nye tool-tester.

- [ ] **Step 7: Commit og push**
```bash
git add -A
git commit -m "refactor(api): splitt tool-handlers i pakke + dispatcher, nye coach-tools

Slice 1/6 av PR #6-splitten. Backend only: handlers/-pakke per domene,
dispatcher, result_links, chat-orkestrering, migrasjoner 011/017/018.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git push -u origin split/1-api-tools
```

- [ ] **Step 8: Åpne PR (base: main)**
```bash
gh pr create --base main --head split/1-api-tools \
  --title "refactor(api): tool-handlers i pakke + dispatcher + nye coach-tools (splitt 1/6)" \
  --body "Slice 1/6 av #6. Kun backend: handler-pakke per domene, dispatcher, result_links, chat-orkestrering, migrasjoner 011/017/018. Merges i rekkefølge 1→6.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

---

### Task 2: `split/2-exercise-db` — exercise-DB v2 + web-foundation

**Files:**
- Backend: slett `api/db/exercises_wger.json` + `api/db/seed_wger.py`; nye `api/db/migrations/014_exercises_v2.sql`, `api/db/seed_free_exercise_db.py`, `api/tests/test_seed_free_exercise_db.py`
- Lim: `docs/ARCHITECTURE.md` (+ rad 014)
- Web-foundation: `web/package.json`, `web/package-lock.json`, `web/vitest.setup.ts`, `web/src/app/globals.css`, `web/src/app/layout.tsx`, `web/src/components/brand/Icon.tsx`, `web/src/lib/api.ts`
- Exercise-web: `web/src/lib/exercises.ts`, `web/src/app/(tabs)/exercises/page.tsx`, `web/src/components/exercises/` (alle 4), `web/public/exercises/sample-bench-press.gif`; slett `web/src/app/exercises/[id]/{layout,page}.tsx`

- [ ] **Step 1: Opprett branch fra split/1**
```bash
git checkout -b split/2-exercise-db split/1-api-tools
```

- [ ] **Step 2: Hent slice-filene**
```bash
git checkout feat/onboarding-redesign -- \
  api/db/migrations/014_exercises_v2.sql api/db/seed_free_exercise_db.py \
  api/tests/test_seed_free_exercise_db.py \
  web/package.json web/package-lock.json web/vitest.setup.ts \
  web/src/app/globals.css web/src/app/layout.tsx \
  web/src/components/brand/Icon.tsx web/src/lib/api.ts \
  web/src/lib/exercises.ts 'web/src/app/(tabs)/exercises/page.tsx' \
  web/src/components/exercises/ web/public/exercises/
git rm -q api/db/exercises_wger.json api/db/seed_wger.py \
  'web/src/app/exercises/[id]/layout.tsx' 'web/src/app/exercises/[id]/page.tsx'
```

- [ ] **Step 3: Installer ev. nye deps og kjør make check**
```bash
cd web && npm ci && cd .. && make check
```
Forventet: exit 0. Risiko: `layout.tsx`/`globals.css` (branch-versjon) kan referere fonter/klasser brukt av senere slices — det er bare ubrukt CSS, harmløst. Ved manglende import: fallback-regelen.

- [ ] **Step 4: Commit, push, PR (base: split/1-api-tools)**
```bash
git add -A
git commit -m "feat(exercises): exercise-db v2 (free-exercise-db) + web-foundation

Slice 2/6 av PR #6-splitten. Migrasjon 014, ny seed-pipeline, exercise-
bibliotek/modal i web, samt delt web-foundation (brand/Icon, api.ts,
globals.css, deps).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git push -u origin split/2-exercise-db
gh pr create --base split/1-api-tools --head split/2-exercise-db \
  --title "feat(exercises): exercise-db v2 + web-foundation (splitt 2/6)" \
  --body "Slice 2/6 av #6. Stacked på splitt 1/6 — merges etter den.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

---

### Task 3: `split/3-program` — program-tab restrukturering

**Files:**
- Migrasjoner: `012_program_folders.sql`, `013_workouts_program_day.sql`, `015_program_day_schedule_and_notes.sql`, `016_program_exercise_sets_notes.sql`
- Routere: `api/app/routers/programs.py`, `api/app/routers/program_folders.py` (ny), `api/app/routers/workouts.py`
- Lim: `api/app/main.py` (+ program_folders import/registrering), `api/tests/conftest.py` (+ program_folders-patchen), `docs/ARCHITECTURE.md` (+ rader 012/013/015/016)
- Tester: `test_programs_router.py`, `test_program_folders_router.py`, `test_active_program_router.py`, `test_add_day_router.py`, `test_add_exercise_router.py`, `test_create_program_router.py`, `test_delete_day_router.py`, `test_update_day_router.py`, `test_update_program_exercise_router.py`, `test_sets_router.py`, `test_delete_logged_set.py`, `test_last_logged_router.py`, `test_workouts_router.py`, `test_migration_015.py`, `test_migration_016.py`
- Web: slett `web/src/components/program/{ExerciseDetail,ExerciseLibrary,ProgramDetail,ProgramList,ProgramScreen,RestTimer}.tsx`; nye `web/src/components/program/{detail,library,wizard,workout}/` (alle); `web/src/app/(tabs)/program/page.tsx`, `[programId]/page.tsx` (ny), `new/page.tsx` (ny); `web/src/components/log/WorkoutLog.tsx`

- [ ] **Step 1: Opprett branch**
```bash
git checkout -b split/3-program split/2-exercise-db
```

- [ ] **Step 2: Hent slice-filene**
```bash
git checkout feat/onboarding-redesign -- \
  api/db/migrations/012_program_folders.sql api/db/migrations/013_workouts_program_day.sql \
  api/db/migrations/015_program_day_schedule_and_notes.sql api/db/migrations/016_program_exercise_sets_notes.sql \
  api/app/routers/programs.py api/app/routers/program_folders.py api/app/routers/workouts.py \
  api/tests/test_programs_router.py api/tests/test_program_folders_router.py \
  api/tests/test_active_program_router.py api/tests/test_add_day_router.py \
  api/tests/test_add_exercise_router.py api/tests/test_create_program_router.py \
  api/tests/test_delete_day_router.py api/tests/test_update_day_router.py \
  api/tests/test_update_program_exercise_router.py api/tests/test_sets_router.py \
  api/tests/test_delete_logged_set.py api/tests/test_last_logged_router.py \
  api/tests/test_workouts_router.py api/tests/test_migration_015.py api/tests/test_migration_016.py \
  web/src/components/program/detail/ web/src/components/program/library/ \
  web/src/components/program/wizard/ web/src/components/program/workout/ \
  'web/src/app/(tabs)/program/page.tsx' 'web/src/app/(tabs)/program/[programId]/page.tsx' \
  'web/src/app/(tabs)/program/new/page.tsx' web/src/components/log/WorkoutLog.tsx
git rm -q web/src/components/program/ExerciseDetail.tsx web/src/components/program/ExerciseLibrary.tsx \
  web/src/components/program/ProgramDetail.tsx web/src/components/program/ProgramList.tsx \
  web/src/components/program/ProgramScreen.tsx web/src/components/program/RestTimer.tsx
```

- [ ] **Step 3: Lim — main.py og conftest.py**

I `api/app/main.py`, legg til (Edit):
```python
from app.routers import program_folders
# ...og:
app.include_router(program_folders.router, prefix="/api")
```
I `api/tests/conftest.py`, legg til i `patch_auth`:
```python
    monkeypatch.setattr("app.routers.program_folders.get_current_user_id", lambda r: TEST_USER_ID)
```

- [ ] **Step 4: make check**
```bash
make check
```
Kjente risikoer: program-komponenter kan importere `lib/tool-labels` eller home-komponenter (slice 6) — bruk fallback-regelen (tool-labels er løvfil, kan flyttes hit).

- [ ] **Step 5: Commit, push, PR (base: split/2-exercise-db)**
```bash
git add -A
git commit -m "feat(program): program-tab med mapper, wizard, detail og workout-run

Slice 3/6 av PR #6-splitten. Migrasjoner 012/013/015/016, program_folders-
router, restrukturert program-UI (library/detail/wizard/workout).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git push -u origin split/3-program
gh pr create --base split/2-exercise-db --head split/3-program \
  --title "feat(program): program-tab restrukturering (splitt 3/6)" \
  --body "Slice 3/6 av #6. Stacked på splitt 2/6.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

---

### Task 4: `split/4-onboarding-login` — onboarding-wizard + login-redesign

**Files:**
- `api/db/migrations/010_onboarding_status.sql`, `api/app/routers/users.py`
- Tester: `test_users_router.py`, `test_complete_onboarding_router.py`
- Lim: `docs/ARCHITECTURE.md` (+ rad 010)
- Web: `web/src/app/onboarding/` (alt: wizard, components, steps, config, storage + tester), `web/src/components/login/` (4 filer), `web/src/app/login/{page,forgot-password/page,reset-password/page}.tsx`, `web/src/app/auth/callback/route.ts` (ny), `web/src/middleware.ts`

- [ ] **Step 1–2: Branch + hent filer**
```bash
git checkout -b split/4-onboarding-login split/3-program
git checkout feat/onboarding-redesign -- \
  api/db/migrations/010_onboarding_status.sql api/app/routers/users.py \
  api/tests/test_users_router.py api/tests/test_complete_onboarding_router.py \
  web/src/app/onboarding/ web/src/components/login/ \
  web/src/app/login/ web/src/app/auth/ web/src/middleware.ts
```

- [ ] **Step 3: make check**
```bash
make check
```

- [ ] **Step 4: Commit, push, PR (base: split/3-program)**
```bash
git add -A
git commit -m "feat(onboarding): onboarding-wizard + login-redesign + middleware-gating

Slice 4/6 av PR #6-splitten. Migrasjon 010, complete_onboarding-endepunkt,
wizard med steg/lagring, nytt login-UI, auth-callback.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git push -u origin split/4-onboarding-login
gh pr create --base split/3-program --head split/4-onboarding-login \
  --title "feat(onboarding): onboarding-wizard + login-redesign (splitt 4/6)" \
  --body "Slice 4/6 av #6. Stacked på splitt 3/6.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

---

### Task 5: `split/5-profil` — profil-redesign

**Files:**
- `web/src/app/(tabs)/profile/{page.tsx,ProfileClient.tsx,ProfileSettings.tsx}` (siste er ny, 1021 linjer)
- `web/src/components/profile/` (AvatarCropModal ny + 5 modifiserte + `sheets/` 7 modifiserte)

- [ ] **Step 1–2: Branch + hent filer**
```bash
git checkout -b split/5-profil split/4-onboarding-login
git checkout feat/onboarding-redesign -- \
  'web/src/app/(tabs)/profile/' web/src/components/profile/
```

- [ ] **Step 3: make check**
```bash
make check
```
Risiko: ProfileSettings kan importere body-metrics-API (typer fra api.ts er allerede inne fra slice 2) eller PWA-ressurser — fallback-regelen ved behov.

- [ ] **Step 4: Commit, push, PR (base: split/4-onboarding-login)**
```bash
git add -A
git commit -m "feat(profil): profil-redesign med ProfileSettings, sheets og avatar-crop

Slice 5/6 av PR #6-splitten.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git push -u origin split/5-profil
gh pr create --base split/4-onboarding-login --head split/5-profil \
  --title "feat(profil): profil-redesign (splitt 5/6)" \
  --body "Slice 5/6 av #6. Stacked på splitt 4/6.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

---

### Task 6: `split/6-rest` — chat-UI, body metrics, historikk/kalender, hjem, PWA, docs

Alt gjenstående, tatt mekanisk slik at treet blir identisk med `feat/onboarding-redesign`. Innholdsmessig: coach/chat-UI (ChatBody, CoachClient, ThinkingDots, ToolUsePill, tool-labels, coach-stream), body metrics (router + side + test), historikk-/kalender-sider, hjem-skjerm, BottomNav/(tabs)-layout, PWA (manifest, ikoner), `.gitignore`, docs (planer, specs, ARCHITECTURE, follow-ups).

- [ ] **Step 1: Branch + mekanisk full-synk mot feature-branchen**
```bash
git checkout -b split/6-rest split/5-profil
git rm -rq .
git checkout feat/onboarding-redesign -- .
git add -A
```

- [ ] **Step 2: Verifiser tre-identitet med feature-branchen**
```bash
git diff --stat feat/onboarding-redesign
```
Forventet: HELT tom output (planfilen er nå committet på branchen). Alt annet er en feil i tidligere slices — stopp og undersøk.

- [ ] **Step 3: make check**
```bash
make check
```
Forventet: exit 0, 130 web-tester, 265 backend-tester — identisk med baseline på feature-branchen.

- [ ] **Step 4: Commit, push, PR (base: split/5-profil)**
```bash
git commit -m "feat(web): chat-UI, body metrics, historikk/kalender, hjem og PWA

Slice 6/6 av PR #6-splitten. Toppen av stacken er tre-identisk med
feat/onboarding-redesign (verifisert med git diff).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git push -u origin split/6-rest
gh pr create --base split/5-profil --head split/6-rest \
  --title "feat(web): chat-UI, body, historikk, hjem, PWA (splitt 6/6)" \
  --body "Slice 6/6 av #6. Tre-identisk med feat/onboarding-redesign. Stacked på splitt 5/6.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

---

### Task 7: Rydd opp PR #6

- [ ] **Step 1: Lukk PR #6 med henvisning til stacken** (etter at alle 6 split-PR-er er åpne)
```bash
gh pr close 6 --comment "Splittet i 6 stablede PR-er (merges i rekkefølge): se splitt 1/6–6/6. Toppen av stacken (split/6-rest) er tre-identisk med denne branchen. Branchen beholdes som referanse til split/6 er merget."
```
Branchen `feat/onboarding-redesign` slettes IKKE før `split/6-rest` er merget til main.

**Merge-prosedyre (når Trym godkjenner, ikke del av denne planen):** Merge PR-ene i rekkefølge 1→6. Etter hver merge: retarget neste PR til `main` (`gh pr edit <n> --base main`). GitHub gjør dette automatisk ved branch-sletting, men verifiser.

**Gjenstående review-funn (H4–H6, M1–M8 i `docs/follow-ups/code-review.md`) fikses ETTER splitten**, som egne commits på den relevante split-branchen (eller på main etter merge).
